import 'package:flutter/material.dart';

class Todo_Screan extends StatelessWidget {
  const Todo_Screan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text("Todo screen"),
    ));
  }
}
